overwrite: true
schema: "./relatorio-schema.graphql"
generates:
  src/types/relatorio-graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      skipTypename: true
